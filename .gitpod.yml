image: elizabethdev/gitpod:infrastructure

tasks:
  - init: |
      terraform init
    command: |
      echo 'TODO: start app'

vscode:
  extensions:
    - hashicorp.terraform
    - njzy.stats-bar
    - esbenp.prettier-vscode
